<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.tour.mapper.ServiceMapper">
	<select id="selectAccomList" resultType="com.spring.tour.vo.Accom_serviceVo" parameterType="string">
		select * from accom_service where user_id=#{user_id}
	</select>
	<select id="selectTourList" resultType="com.spring.tour.vo.TourServiceVo" parameterType="string">
		select * from tour_service where user_id=#{user_id}
	</select>
	<insert id="insertAccomService" parameterType="com.spring.tour.vo.Accom_serviceVo">
		insert into accom_service values((select nvl(max(accom_service_number)+1,0) from accom_service),#{cate_number},#{user_accom_boss_name},#{accom_name},#{accom_addr})
	</insert>
	<insert id="insertAccomInfo" parameterType="com.spring.tour.vo.AccomInfoVo">
		insert into accom_info values((select nvl(max(accom_info_number)+1,0) from accom_info),(select nvl(max(accom_service_number),0) from accom_service),#{accom_info_content},#{accom_how},#{accom_rule},#{accom_chekinfo},#{facility},#{conven})
	</insert>
</mapper>