<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.tour.mapper.AccomMapper">
	<select id="accom_search" resultType="accomService" parameterType="hashmap">
		select * from accom_service where
		accom_service_number in(
		select accom_service_number from accom_option left join accom_book 
		on accom_option.accom_option_number = accom_book.accom_option_number
		<where>
		<![CDATA[
		not (accom_startdate < #{endDate}
		and accom_enddate > #{startDate})
		or (accom_startdate is null and accom_enddate is null)
		and accom_max_people>=#{count}
		)
		]]>
		<if test="facility!=null">
		and accom_service_number in(
		select accom_service_number from accom_service join facilities on accom_service_number=facilities.service_number and 
		accom_service.cate_number=facilities.cate_number where facility in
		<foreach collection="facility" item="fitem" index='i' open="(" close=")" separator=",">
		#{fitem}
		</foreach>
		)
		</if>
		<if test="conven!=null">
		and accom_service_number in(
		select accom_service_number from accom_service join convenience on accom_service_number=convenience.service_number and 
		accom_service.cate_number=convenience.cate_number where conven in
		<foreach collection="conven" item="citem" index='i' open="(" close=")" separator=",">
		#{citem}
		</foreach>
		)
		</if>
		<if test="minprice!=null">
		<![CDATA[
		and accom_service_number in(
		select accom_service_number from accom_option left join accom_book 
		on accom_option.accom_option_number = accom_book.accom_option_number
		where accom_price>=#{minprice} and accom_price<=#{maxprice})
		]]>
		</if>
		<if test="category!=null">
		and accom_service_number in(
		select accom_service_number from accom_service natural join category 
		where cate_number=#{category})
		</if>
		</where>
	</select>
	<select id="accom_minprice_search" parameterType="hashmap" resultType="hashmap">
		select min(accom_price) minp,max(accom_price) maxp from accom_option group by accom_service_number
		having accom_service_number in
		<foreach collection="snum" item='item' index='i' open="(" close=")" separator=",">
		 #{item}
		</foreach>
	</select>
</mapper>